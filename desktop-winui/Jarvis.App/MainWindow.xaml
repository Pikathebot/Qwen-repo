<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Jarvis_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Jarvis_App"
    xmlns:models="using:Jarvis.Core.Models"
    xmlns:views="using:Jarvis_App.Views"
    xmlns:glass="using:Jarvis_Glass"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Jarvis - Local AI Command Center"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--
            Ambient ground. The acrylic behind this window resolves to a flat grey whenever the
            desktop behind it has no detail to sample (a solid-colour desktop, or a live-wallpaper
            tool painting a placeholder), which leaves the gaps between panels looking dead and
            makes the glass slabs read as cards pasted on cardboard. These two layers give the
            shell its own depth: a void gradient falling away toward the bottom right, and a cool
            light source in the top-left corner - the same corner every panel's bevel is lit from,
            so the whole shell agrees about where the light comes from.
        -->
        <Grid Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False">
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Offset="0" Color="#BF0B1220" />
                        <GradientStop Offset="0.55" Color="#D9070A12" />
                        <GradientStop Offset="1" Color="#E604060B" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <Border>
                <Border.Background>
                    <RadialGradientBrush Center="0.12,0.04" GradientOrigin="0.12,0.04" RadiusX="0.85" RadiusY="0.85">
                        <GradientStop Offset="0" Color="#2606B6D4" />
                        <GradientStop Offset="0.45" Color="#0F1E90A8" />
                        <GradientStop Offset="1" Color="#0006B6D4" />
                    </RadialGradientBrush>
                </Border.Background>
            </Border>
        </Grid>

        <!-- Custom title bar: draggable region, app name left, window controls handled by AppWindow. -->
        <Grid x:Name="TitleBarRow" Grid.Row="0" Background="Transparent">
            <TextBlock
                Text="JARVIS"
                Foreground="#06B6D4"
                FontWeight="SemiBold"
                FontSize="13"
                Margin="16,0,0,0"
                VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" Padding="12,0,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="RightPanelColumn" Width="0" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar: floating glass slab. ShaderEnabled opts this hero surface into Tier A. -->
            <glass:GlassPanel Grid.Column="0" Margin="0,0,8,0" GlassCornerRadius="18" ShaderEnabled="True">
                <glass:GlassPanel.GlassContent>
                    <StackPanel Padding="16" Spacing="12">
                        <TextBlock Text="Jarvis" Foreground="White" FontSize="18" FontWeight="Bold" />

                        <ComboBox
                            x:Name="ProjectSwitcher"
                            HorizontalAlignment="Stretch"
                            PlaceholderText="Default Workspace"
                            ItemsSource="{x:Bind ProjectsViewModel.Projects}"
                            SelectedItem="{x:Bind ProjectsViewModel.ActiveProject, Mode=OneWay}"
                            SelectionChanged="ProjectSwitcher_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="models:Project">
                                    <TextBlock Text="{x:Bind Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <Button Content="+ New Chat" HorizontalAlignment="Stretch" Click="NewChat_Click" />
                        <TextBlock Text="Recent Sessions" Foreground="#94A3B8" FontSize="12" Margin="0,12,0,0" />
                        <ListView
                            x:Name="SessionsList"
                            Background="Transparent"
                            SelectionMode="None"
                            ItemsSource="{x:Bind SessionsViewModel.Sessions}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:Session">
                                    <Grid Padding="8,6" Tapped="SessionItem_Tapped" Tag="{x:Bind}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Text="{x:Bind SessionId}"
                                            Foreground="#CBD5E1"
                                            FontSize="12"
                                            TextTrimming="CharacterEllipsis" />
                                        <Button
                                            Grid.Column="1"
                                            Content="✕"
                                            FontSize="10"
                                            Padding="4"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Tag="{x:Bind}"
                                            Click="DeleteSession_Click" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <Button Content="Free VRAM" HorizontalAlignment="Stretch" Click="FreeVram_Click" />
                        <Button Content="Settings" HorizontalAlignment="Stretch" Click="Settings_Click" />
                    </StackPanel>
                </glass:GlassPanel.GlassContent>
            </glass:GlassPanel>

            <!-- Chat surface -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header: session label, mode toggle, governor pill -->
                <glass:GlassPanel Grid.Row="0" Margin="0,0,0,8" GlassCornerRadius="14" ShaderEnabled="True">
                    <glass:GlassPanel.GlassContent>
                        <Grid Padding="16,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="SessionLabel" Text="Default Workspace" Foreground="White" VerticalAlignment="Center" />
                            <Button Grid.Column="1" Margin="0,0,12,0" Padding="10,4" Background="Transparent" BorderThickness="1">
                                <TextBlock x:Name="GovernorPillText" Text="Offline" Foreground="#F43F5E" FontSize="12" />
                                <Button.Flyout>
                                    <Flyout Placement="Bottom">
                                        <StackPanel Spacing="8" MinWidth="220">
                                            <TextBlock x:Name="GovernorBackendRow" Text="Backend: —" FontSize="12" Foreground="#CBD5E1" />
                                            <TextBlock x:Name="GovernorModelRow" Text="Model: —" FontSize="12" Foreground="#CBD5E1" />
                                            <TextBlock x:Name="GovernorStatusRow" Text="Status: —" FontSize="12" Foreground="#CBD5E1" />
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <Button x:Name="GovernorPauseButton" Content="Pause Governor" Click="GovernorPause_Click" />
                                                <Button Content="Resume Governor" Click="GovernorResume_Click" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                            <Button x:Name="HudButton" Grid.Column="2" Content="HUD" Margin="0,0,8,0" Click="HudButton_Click" />
                            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8">
                                <Button x:Name="MicButton" Content="🎙" FontSize="14" Padding="8,4" Click="MicButton_Click" />
                                <TextBlock x:Name="VoiceStateText" Text="" Foreground="#64748B" FontSize="11" VerticalAlignment="Center" />
                                <Button Content="Panel" Click="ToggleRightPanel_Click" />
                            </StackPanel>
                        </Grid>
                    </glass:GlassPanel.GlassContent>
                </glass:GlassPanel>

                <!-- Message list -->
                <glass:GlassPanel Grid.Row="1" GlassCornerRadius="18" ShaderEnabled="True">
                    <glass:GlassPanel.GlassContent>
                        <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <InfoBar
                            x:Name="ErrorBanner"
                            Grid.Row="0"
                            Severity="Error"
                            IsOpen="False"
                            Margin="12,12,12,0"
                            CornerRadius="10" />
                        <ListView
                            x:Name="MessageList"
                            Grid.Row="1"
                            Padding="16"
                            SelectionMode="None"
                            ItemsSource="{x:Bind ChatViewModel.Messages}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <!--
                                    No x:DataType/x:Bind here deliberately: MessageBubbleControl
                                    renders via classic Binding/DataContext (see its code-behind),
                                    since it must re-render markdown on every streamed token
                                    change, not just once at bind time.
                                -->
                                <DataTemplate>
                                    <views:MessageBubbleControl HorizontalAlignment="Stretch" Loaded="MessageBubble_Loaded" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        </Grid>
                    </glass:GlassPanel.GlassContent>
                </glass:GlassPanel>

                <!-- Composer -->
                <glass:GlassPanel Grid.Row="2" Margin="0,8,0,0" GlassCornerRadius="14" ShaderEnabled="True">
                    <glass:GlassPanel.GlassContent>
                        <StackPanel Padding="12" Spacing="6">
                            <ItemsControl x:Name="AttachmentChips">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="6" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Content="📎" FontSize="14" Padding="8,4" Click="AttachFile_Click" />
                                <TextBox
                                    x:Name="ComposerBox"
                                    Grid.Column="1"
                                    Margin="8,0"
                                    PlaceholderText="Message Jarvis..."
                                    AcceptsReturn="True"
                                    TextWrapping="Wrap"
                                    MaxHeight="160"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    KeyDown="ComposerBox_KeyDown" />
                                <Button
                                    x:Name="SendButton"
                                    Grid.Column="2"
                                    Content="Send"
                                    Click="SendButton_Click" />
                            </Grid>
                        </StackPanel>
                    </glass:GlassPanel.GlassContent>
                </glass:GlassPanel>

                <!-- Awareness tray: floats above the composer, bottom-right of the chat column -->
                <ContentControl
                    x:Name="AwarenessTrayHost"
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,0,8,8"
                    IsTabStop="False" />
            </Grid>

            <!-- Right panel: overlay sheet with Artifacts + Files tabs -->
            <glass:GlassPanel
                x:Name="RightPanel"
                Grid.Column="2"
                Margin="8,0,0,0"
                GlassCornerRadius="18"
                ShaderEnabled="True"
                Visibility="Collapsed">
                <glass:GlassPanel.GlassContent>
                    <TabView IsAddTabButtonVisible="False" Background="Transparent">
                        <TabViewItem Header="Artifacts" IsClosable="False">
                            <Grid Padding="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Button Content="Refresh" HorizontalAlignment="Stretch" Click="RefreshArtifacts_Click" />
                                <ListView
                                    x:Name="ArtifactsList"
                                    Grid.Row="1"
                                    MaxHeight="160"
                                    ItemsSource="{x:Bind RightPanelViewModel.Artifacts}"
                                    SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:Artifact">
                                            <Grid Padding="8,6" Tapped="ArtifactItem_Tapped" Tag="{x:Bind}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind Name}" Foreground="#CBD5E1" FontSize="12" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="1" Text="{x:Bind Type}" Foreground="#64748B" FontSize="10" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <ScrollViewer Grid.Row="2" Margin="0,8,0,0">
                                    <TextBlock
                                        x:Name="ArtifactContent"
                                        Text="{x:Bind RightPanelViewModel.SelectedContent, Mode=OneWay}"
                                        FontFamily="Cascadia Mono, Consolas, monospace"
                                        FontSize="12"
                                        Foreground="#E2E8F0"
                                        TextWrapping="Wrap"
                                        IsTextSelectionEnabled="True" />
                                </ScrollViewer>
                            </Grid>
                        </TabViewItem>
                        <TabViewItem Header="Files" IsClosable="False">
                            <Grid Padding="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Button Content="Refresh" HorizontalAlignment="Stretch" Click="RefreshFiles_Click" />
                                <ListView
                                    Grid.Row="1"
                                    Margin="0,8,0,0"
                                    ItemsSource="{x:Bind RightPanelViewModel.Files}"
                                    SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:ProjectFile">
                                            <TextBlock Text="{x:Bind Name}" Foreground="#CBD5E1" FontSize="12" Padding="8,4" />
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </TabViewItem>
                        <TabViewItem Header="Context" IsClosable="False">
                            <ScrollViewer Padding="12">
                                <StackPanel Spacing="10">
                                    <TextBlock x:Name="ContextBudgetHeader" Text="No turn yet" Foreground="White" FontSize="13" FontWeight="SemiBold" />
                                    <Grid x:Name="ContextBudgetGrid" ColumnSpacing="8" RowSpacing="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="Tier1Text" Text="Tier1 System: —" FontSize="11" Foreground="#94A3B8" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="Tier2Text" Text="Tier2 User+Files: —" FontSize="11" Foreground="#94A3B8" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" x:Name="Tier3Text" Text="Tier3 RAG: —" FontSize="11" Foreground="#94A3B8" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="Tier4Text" Text="Tier4 History: —" FontSize="11" Foreground="#94A3B8" />
                                    </Grid>
                                    <TextBlock Text="Retrieved Code Chunks" Foreground="#64748B" FontSize="11" Margin="0,8,0,0" />
                                    <ItemsControl x:Name="RetrievedChunksList">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Spacing="6" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </ScrollViewer>
                        </TabViewItem>
                        <TabViewItem Header="Activity" IsClosable="False">
                            <ScrollViewer Padding="12">
                                <ItemsControl x:Name="ActivityStepsList">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="6" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </ScrollViewer>
                        </TabViewItem>
                    </TabView>
                </glass:GlassPanel.GlassContent>
            </glass:GlassPanel>
        </Grid>
    </Grid>
</Window>
