<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Jarvis_Glass.LiquidGlassCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        CanvasControl is sealed, so this wraps one rather than subclassing it — see the remark in
        LiquidGlassCanvas.xaml.cs for what it draws and why it can do real refraction where the
        Composition-based Tier B cannot.
    -->
    <canvas:CanvasControl x:Name="Canvas" />
</UserControl>
